namespace Astrolabe.Domain.Features.Catalog.Enums;

/// <summary>Which way a listing is ordered. The prototype toggles it by clicking a header twice.</summary>
public enum SortDirection
{
    Ascending = 0,
    Descending = 1
}
